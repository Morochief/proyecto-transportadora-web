fix: multiple corrections in auth, PDF, and forms

- Auth: refresh returns 401 instead of 400 without token
- Cookie: secure=false in dev (HTTP)
- Admin seed: configurable password via DEFAULT_ADMIN_PASSWORD
- Entrypoint: fixed CRLF to LF (Windows compat)
- node_modules: anonymous volume preserves installation
- bootstrapSession: skip refresh when no prior session
- Paises/Ciudades: migrated from fetch() to Axios with auth
- ModalMIC: fetch() migrated to Axios; unified chofer key
- PDF MIC: field 9 with name+address, precints in field 37
- PDF MIC: field 38 without truncation (removed [:1500])
- PDF MIC: unified font for field 15 rows
- PDF MIC: aligned grid (columns 55|366|470|916|1305|1671)
- PDF MIC: field 40 includes chofer
- Fixed encoding corruption in several files
